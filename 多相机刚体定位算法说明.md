# 多相机刚体定位算法说明

## 1. 文档目的

本文汇总本项目从同步图片或视频输入，到Marker二维检测、身份分配、三维重建、刚体位姿计算、6DoF联合优化和误差统计的完整流程。

这套方法不绑定固定的相机数量、相机位置、工作距离或Marker尺寸。相机和Marker均由配置文件描述，算法按当前帧实际有效的观测数量运行。文中同时区分：

- **当前离线评估实现**：使用GT辅助分配Marker编号，用GT计算误差；
- **真实在线系统**：不能读取GT，需要依靠初始化和上一帧位姿完成Marker编号，运行时只输出位姿及内部质量指标。

## 2. 总体流程

```text
同步图片或视频
    ↓
读取相机标定与刚体Marker模板
    ↓
每台相机独立检测二维Marker圆心
    ↓
给检测点分配Marker编号
    ↓
同名Marker跨相机关联
    ↓
多视图DLT三角化
    ↓
Kabsch计算初始刚体位姿
    ↓
所有相机、所有Marker联合优化6DoF位姿
    ↓
输出逐帧位置、姿态和质量指标
    ↓
有GT时计算二维、三维和位姿误差
```

DLT和Kabsch负责快速得到可靠初值，6DoF联合优化负责直接利用全部二维观测进一步修正位姿。联合优化不会替代前两步，而是接在它们后面。

## 3. 输入数据

### 3.1 在线定位必需输入

1. 多台相机的同步图片或视频帧；
2. 每台相机的内参矩阵、畸变参数和外参；
3. Marker刚体模板，即每个Marker在刚体局部坐标系中的固定三维位置；
4. 第一帧的初始化信息，或者上一帧位姿；
5. 图像检测参数，例如颜色范围、亮度范围和候选区域尺寸范围。

### 3.2 离线误差评估额外输入

1. Marker二维GT；
2. Marker三维GT；
3. 刚体位置和姿态GT；
4. 可选的物理遮挡、可见性和观察角度标注。

GT只用于离线验证、Marker辅助编号和误差计算，不属于真实在线定位系统的输入。

## 4. 通用条件与最低要求

### 4.1 相机数量

算法从`camera.json`读取相机列表并循环处理，不在算法中固定为4、8、12、16或20台。

- 单个Marker至少需要被两台已标定相机看到，才能通过多视图方法计算三维位置；
- 相机数量增加通常会增加可用观测，但错误标定、过小视差或带偏差的观测也可能使结果变差；
- 真正有用的是观察方向、视差、覆盖范围和标定质量，而不只是相机总数；
- 某一帧不要求所有相机都看到目标，只使用该帧实际有效的相机。

### 4.2 Marker数量与布局

- Kabsch位姿初始化至少需要3个已重建、互不共线的Marker；
- Marker数量可以多于3个，多余Marker共同参与拟合；
- Marker应固定在刚体上，局部坐标不能随时间改变；
- Marker分布越分散、越不对称、越不共面，姿态通常越容易稳定估计；
- Marker是否叫H01～H06或B01～B06不影响算法，编号只需在模板和观测中保持一致。

### 4.3 Marker大小和工作距离

Marker物理尺寸不直接写死在位姿算法中，但会影响图像中的像素尺寸和二维检测精度。近似关系为：

\[
d_{px}\approx \frac{f_{px}D}{Z}
\]

其中，\(D\)是Marker实际直径，\(Z\)是相机到Marker的深度，\(f_{px}\)是以像素表示的焦距，\(d_{px}\)是预计图像直径。

因此不能对所有相机、所有距离永久使用同一个面积门限。当前脚本允许配置最小和最大连通域面积，并提供门限扫描工具；真实系统建议根据焦距、距离预测和Marker直径动态给出合理范围。

## 5. 相机模型和坐标系统

### 5.1 相机投影矩阵

对第\(i\)台相机，投影矩阵为：

\[
P_i=K_i[R_i\mid t_i]
\]

其中：

- \(K_i\)：相机内参，包含焦距和主点；
- \(R_i,t_i\)：世界坐标到相机坐标的外参；
- \(P_i\)：把世界三维点投影到该相机图像的矩阵。

世界点\(X_w\)投影到图像后：

\[
\tilde u=P_i\tilde X_w,\qquad
u=(\tilde u_x/\tilde u_z,\tilde u_y/\tilde u_z)
\]

### 5.2 坐标和单位要求

- 相机外参、刚体模板、三维GT和位姿必须处于一致的坐标定义下；
- 当前数据通常以厘米存储三维坐标，报告时换算为毫米；
- 旋转矩阵必须是正交矩阵且行列式接近1；
- 如果相机存在镜头畸变，应先去畸变，或者在投影模型中显式使用畸变参数；
- 主点采用\((w-1)/2,(h-1)/2\)还是\((w/2,h/2)\)必须与生成或标定过程一致。

坐标系不一致会形成持续的固定方向偏差，联合优化通常无法从根本上消除这种系统错误。

## 6. 帧同步与读取

### 6.1 图片输入

无损PNG保留了原始像素边缘，适合离线精度评估。程序按相机编号和帧编号读取同一时刻的图像。

### 6.2 视频输入

视频通过OpenCV按顺序解码。所有相机必须满足：

- 起始时刻一致；
- 帧率一致；
- 帧编号对应同一实际时间；
- 不存在某一路额外丢帧或重复帧。

H.264压缩可能改变Marker边缘、颜色和亚像素圆心，因此视频结果与PNG结果应分别记录。

当前评估脚本支持：

```bash
--input-source auto
--input-source png
--input-source video
```

`auto`在所有相机PNG都存在时优先使用PNG。

## 7. 二维Marker检测

### 7.1 当前红色Marker检测

当前离线数据使用红色Marker，处理步骤为：

1. 将BGR图像转换到HSV空间；
2. 提取低色相和高色相两段红色区域；
3. 使用3×3闭运算连接小裂缝；
4. 计算连通区域；
5. 根据面积范围保留候选；
6. 对每个候选计算亚像素圆心。

当前HSV范围为：

```text
H: 0～25 或 165～179
S: 45～255
V: 100～255
```

真实NIR反光Marker可以替换为灰度、亮度、斑点或学习型检测器。只要检测器最终输出每个候选的二维坐标和质量信息，后续三角化与位姿流程无需改变。

### 7.2 圆心计算方法

当前代码支持以下方法：

#### 普通质心

对二值区域中所有像素等权计算中心。速度快，但边缘缺失或压缩噪声会直接拉动中心。

#### 红色强度加权圆心

像素权重为：

\[
w=\max\left(R-\frac{G+B}{2},1\right)
\]

红色特征更强的像素权重更高，可以减弱暗边缘和轻微背景混入的影响。目前多数正式测试使用这一方法。

#### 椭圆拟合

Marker被斜着观察时通常呈椭圆。算法提取轮廓并拟合椭圆，使用拟合椭圆的中心作为Marker圆心。

#### 椭圆混合模式

仅在轮廓点数、长短轴比例、填充率、中心移动量等条件合理时采用椭圆中心，否则回退到普通质心。该方式用于避免小目标、严重缺边或局部遮挡时产生不稳定拟合。

### 7.3 面积门限不应绑定固定Marker尺寸

固定门限只适合固定分辨率、焦距和距离范围。通用方案可以采用以下方式：

1. 使用较宽的初始面积范围收集候选；
2. 根据上一帧位姿预测当前Marker深度；
3. 根据物理直径和焦距估计当前像素直径；
4. 只保留尺寸与预测范围相符的候选；
5. 目标距离变化较大时，按相机和帧动态更新门限。

离线数据可以使用`scan_detection_area_thresholds.py`扫描候选门限，但不能只看二维RMSE，还要同时检查召回率、三维覆盖率、位置误差和姿态误差。

## 8. Marker身份分配

检测器最初只产生一组没有编号的二维点，后续计算必须知道每个点对应哪个刚体Marker。

### 8.1 当前离线评估方式

当前评估流程使用二维GT坐标构造距离代价矩阵，再通过Hungarian算法完成一对一分配，并使用像素距离门限拒绝过远匹配。

这种方式可以单独评价圆心、三角化和位姿算法，但使用了GT辅助，不能代表完整在线系统。

### 8.2 真实在线方式

真实系统可以使用上一帧位姿\((R_{k-1},t_{k-1})\)完成当前帧编号：

1. 使用运动模型预测当前位姿\((R_k^-,t_k^-)\)；
2. 将每个模板点\(X_j\)变换到预测世界位置；
3. 将它投影到每台相机，得到预测像素位置；
4. 计算预测点与当前检测候选之间的距离代价；
5. 使用Hungarian算法做一对一匹配；
6. 拒绝距离、尺寸、形状或运动速度不合理的匹配；
7. 用跨相机三角化和刚体距离关系再次检查编号；
8. 位姿求解成功后，用新位姿更新下一帧预测。

第一帧没有上一帧位姿时，可以使用人工指定、已知初始区域、PnP/多假设匹配或短时间窗口搜索完成初始化。

上一帧预测编号目前仍属于后续在线化方向，尚未替换离线评估中的GT辅助编号。

## 9. 多视图DLT三角化

同一个Marker在两台或更多相机中完成编号后，可以计算其世界三维坐标。

对相机\(i\)中的观测\((u_i,v_i)\)，投影矩阵三行为\(p_{i1},p_{i2},p_{i3}\)，建立：

\[
(u_ip_{i3}-p_{i1})\tilde X=0
\]

\[
(v_ip_{i3}-p_{i2})\tilde X=0
\]

将所有参与相机的方程堆叠成矩阵\(A\)，通过SVD求解：

\[
\min_{\|\tilde X\|=1}\|A\tilde X\|
\]

取最小奇异值对应的向量并进行齐次归一化，即得到Marker三维点。

### 9.1 有效视角的重要性

两台相机看到Marker并不一定等于三角化质量高。以下情况会放大深度误差：

- 两条观察射线夹角过小；
- 相机位置接近同一直线；
- Marker距离过远、图像尺寸太小；
- 某台相机圆心存在固定方向偏差；
- 相机标定误差或帧不同步。

通用系统应记录参与相机数、射线夹角、重投影误差和三角化条件数，并拒绝明显退化的组合。

## 10. Kabsch刚体初始位姿

假设模板中Marker局部坐标为\(X_j\)，三角化后的世界坐标为\(Y_j\)，刚体关系为：

\[
Y_j\approx RX_j+t
\]

Kabsch算法计算最符合这些对应点的旋转\(R\)和平移\(t\)，且不允许缩放。

处理步骤为：

1. 分别计算局部点和世界点的中心；
2. 两组点分别减去各自中心；
3. 计算协方差矩阵；
4. 对协方差矩阵执行SVD；
5. 组合得到旋转矩阵；
6. 如果出现镜像反射，修正SVD符号；
7. 根据两组中心计算平移。

Kabsch速度快、结果稳定，适合给后续非线性优化提供初值。但它依赖已经三角化出的三维Marker，二维误差会先经过三角化再传递到位姿。

## 11. 6DoF多相机联合优化

### 11.1 优化目标

刚体模板中的Marker位置固定，因此可以直接调整整个刚体的3个旋转自由度和3个平移自由度，让所有有效二维观测同时得到解释：

\[
\min_{R,t}\sum_{(i,j)\in\mathcal O}
\rho\left(
\left\|
\pi(P_i(RX_j+t))-u_{ij}
\right\|^2
\right)
\]

其中：

- \(X_j\)：Marker在刚体局部坐标系中的固定位置；
- \(P_i\)：第\(i\)台相机的投影矩阵；
- \(u_{ij}\)：相机\(i\)检测到的Marker \(j\)二维圆心；
- \(\pi\)：齐次坐标归一化；
- \(\mathcal O\)：当前帧全部有效的相机—Marker观测；
- \(\rho\)：Huber、Cauchy等鲁棒损失。

每个二维观测产生横向和纵向两个残差。当前实现默认所有有效观测等权，不按特定Marker、相机方向或运动方向设置固定权重。

### 11.2 优化如何更新位姿

优化变量是相对Kabsch初值的6维增量：

```text
[旋转增量rx, ry, rz, 平移增量tx, ty, tz]
```

每次迭代执行：

1. 将旋转向量增量转换为旋转矩阵；
2. 更新刚体旋转和平移；
3. 用新位姿计算每个Marker的世界位置；
4. 投影到所有参与相机；
5. 计算预测位置与检测圆心的像素差；
6. 根据全部残差和鲁棒损失决定下一次更新方向；
7. 直到收敛或达到最大迭代次数。

优化目标是所有观测残差的总和，所以它可能让个别相机残差略微增加，以换取整体残差明显降低。Huber损失会降低少量大异常值的影响，避免单个坏观测控制整个位姿。

### 11.3 初始化和回退

当前实现使用DLT+Kabsch位姿作为初值。优化结果只有在以下条件全部满足时才采用：

- 优化器报告成功；
- 6维更新量全部为有限数；
- 最终鲁棒目标函数不高于初始目标函数。

否则自动回退到DLT+Kabsch位姿，避免一次失败优化破坏输出。

### 11.4 为什么保留DLT和Kabsch

- 给非线性优化提供接近正确答案的初值；
- 第一帧或检测质量较差时仍有基础结果；
- 优化失败时可以回退；
- 可以分别定位误差来自二维检测、三角化、刚体拟合还是联合优化。

## 12. 时序信息扩展

当前正式误差结果以逐帧独立位姿为主，尚未用前后帧直接平滑最终结果。真实系统可以增加时序层：

1. 使用上一帧位置、速度和角速度预测当前位姿；
2. 用预测投影完成无GT Marker编号；
3. 把当前多相机观测更新到预测位姿；
4. 对短时漏检保留轨迹而不是立即丢失；
5. 对不符合合理速度或加速度的跳变进行检查。

可选方法包括常速度模型、卡尔曼滤波、扩展卡尔曼滤波、滑动窗口优化和因子图。

时序信息主要提供两个优势：

- 去除GT辅助编号，为当前检测点提供Marker身份预测；
- 降低单帧噪声和短时漏检造成的位姿跳动。

时序约束不能设置得过强，否则快速转头或突然改变路线时会产生延迟。应根据帧率、允许速度和实际运动范围设置过程噪声或约束强度。

## 13. 误差指标

### 13.1 二维圆心误差

检测圆心\(u\)与二维GT \(u^{gt}\)的欧氏距离：

\[
e_{2D}=\|u-u^{gt}\|_2
\]

单位为像素。

### 13.2 三维Marker误差

三角化点\(X\)与三维GT \(X^{gt}\)的距离：

\[
e_{3D}=\|X-X^{gt}\|_2
\]

当前通常从厘米换算为毫米。

### 13.3 位置误差

估计刚体中心\(t\)与位置GT \(t^{gt}\)的距离：

\[
e_t=\|t-t^{gt}\|_2
\]

该值表示整个三维位置相差多少，不只统计某一个坐标轴。

### 13.4 姿态误差

先计算估计旋转与GT旋转之间的相对旋转：

\[
R_{err}=R_{est}R_{gt}^{T}
\]

再取将两者对齐所需的最小旋转角：

\[
e_R=\cos^{-1}\left(\frac{\operatorname{tr}(R_{err})-1}{2}\right)
\]

单位为度。它表示估计朝向需要再旋转多少度才能与真实朝向重合，不是简单地分别相减欧拉角。

### 13.5 统计量

- **Mean**：所有帧绝对误差的平均值；
- **RMSE**：先平方、求平均、再开方，对大误差帧更敏感；
- **P95**：95%的误差不超过该值，用于观察长尾；
- **Max**：最差一帧；
- **Bias**：误差向量的平均值，用于发现持续偏向某个方向的问题；
- **Coverage**：成功形成三维Marker或刚体位姿的比例；
- **Recall**：实际可见或GT投影观测中被检测到的比例。

真实系统没有GT，运行时只能统计检测数量、重投影误差、参与相机数、三角化几何、优化是否收敛和时序连续性。位置与姿态真误差只能在有外部真值的测试阶段获得。

## 14. 参数如何适应不同系统

| 变化条件 | 不需要改变 | 需要调整或重新标定 |
|---|---|---|
| 相机数量变化 | DLT、Kabsch、6DoF目标形式 | `camera.json`和同步读取配置 |
| 相机位置变化 | 整体算法流程 | 每台相机外参、覆盖与视差检查 |
| 焦距或FOV变化 | 刚体求解方法 | 内参、畸变和二维尺寸范围 |
| 图像分辨率变化 | 三角化和位姿结构 | 内参缩放、检测面积和像素门限 |
| Marker大小变化 | DLT、Kabsch、6DoF结构 | 检测面积、预期像素尺寸和曝光 |
| Marker位置变化 | 求解方法 | `rigid_bodies.json`中的局部模板 |
| Marker数量变化 | 求解方法 | 模板、最低有效点数和编号逻辑 |
| 红色改为NIR | 三角化与位姿部分 | 二维检测器和图像采集方式 |
| PNG改为视频 | 后端几何计算 | 解码、同步、压缩影响和检测参数 |

算法通用化的关键不是删除所有参数，而是把物理条件放入标定和配置中，并让每帧只使用实际可靠的观测。

## 15. 当前实现与真实部署的区别

| 环节 | 当前离线实现 | 真实在线系统建议 |
|---|---|---|
| 帧输入 | PNG或顺序解码MP4 | 硬件同步或可靠时间戳同步 |
| 二维检测 | 红色HSV连通域 | NIR/反光点或现场适配检测器 |
| Marker编号 | GT辅助Hungarian | 上一帧位姿预测和刚体一致性 |
| 三角化 | 多视图线性DLT | 归一化DLT、坏视角剔除和非线性细化 |
| 初始位姿 | Kabsch | Kabsch或预测位姿 |
| 最终位姿 | 单帧Huber 6DoF优化 | 联合优化加时序状态估计 |
| 误差统计 | 使用2D、3D和位姿GT | 内部质量指标；外部系统离线验收 |
| ROS原生节点 | 部分代码按双相机设计 | 需要扩展为动态相机列表或使用离线后端 |

## 16. 常见误差来源

1. 相机内外参不准确；
2. 多相机帧不同步；
3. 视频压缩、运动模糊或曝光变化；
4. Marker过小、过曝、欠曝或部分遮挡；
5. 圆形Marker斜视后形成椭圆，轮廓缺失导致中心偏移；
6. Marker模板坐标与实际安装位置不一致；
7. 相机观察射线夹角太小；
8. Marker编号错误；
9. 多个Marker布局接近共线或过于集中；
10. 将图像可见与物理无遮挡混为一谈；
11. 坐标轴、单位、主点或矩阵方向不一致；
12. 给持续的小偏差设置过高信任度。

## 17. 推荐质量检查顺序

出现误差时建议按以下顺序排查：

1. 用三维GT重新投影到二维GT，验证相机矩阵和坐标系；
2. 检查视频帧数、时间戳和跨相机同步；
3. 查看二维检测叠加图，确认圆心是否存在固定方向偏差；
4. 按相机、Marker、距离和轨迹区段统计二维误差；
5. 检查每个Marker的有效相机数量和观察射线夹角；
6. 检查三维Marker误差及P95、最大值；
7. 检查Kabsch初始位置与姿态误差；
8. 比较联合优化前后的位姿和重投影误差；
9. 查看位置Bias，判断是否存在系统偏移；
10. 最后再调整相机布局、Marker朝向或时序参数。

## 18. 代码对应关系

### `scripts/evaluate_dataset_metrics.py`

负责：

- PNG/视频读取；
- 红色Marker检测；
- 多种二维圆心算法；
- GT辅助Hungarian编号；
- 多视图DLT；
- Kabsch初始位姿；
- 二维、三维、位置和姿态基础指标；
- 检测、重建和初始位姿文件输出。

### `scripts/refine_helmet_pose.py`

负责：

- 读取DLT+Kabsch初值；
- 构造所有相机、所有Marker的二维重投影残差；
- 使用`scipy.optimize.least_squares`优化6DoF；
- 支持`linear`、`huber`、`soft_l1`和`cauchy`损失；
- 检查优化是否成功并在必要时回退；
- 输出联合优化位姿和逐帧误差。

### `scripts/scan_detection_area_thresholds.py`

负责：

- 重用已缓存的二维候选；
- 扫描不同连通域面积门限；
- 对比检测数、二维误差、三维覆盖和初始位姿误差；
- 为候选参数生成后续联合优化所需的输入。

## 19. 通用运行示例

### 19.1 基础流程

```bash
python3 scripts/evaluate_dataset_metrics.py DATASET_PATH \
  --input-source video \
  --min-area MIN_AREA \
  --max-area MAX_AREA \
  --center-method red_weighted \
  --match-radius MATCH_RADIUS \
  --output OUTPUT_BASE
```

如果使用PNG，将`--input-source video`改为`png`。

### 19.2 6DoF联合优化

```bash
python3 scripts/refine_helmet_pose.py \
  --dataset DATASET_PATH \
  --algorithm-output OUTPUT_BASE \
  --output OUTPUT_REFINED \
  --loss huber \
  --f-scale HUBER_SCALE_PX \
  --max-nfev MAX_ITERATIONS
```

这些命令中的相机数量由数据集配置自动决定，不需要在命令中写死。

## 20. 输出文件

基础流程主要输出：

- `detections_2d.json`：每帧每台相机的候选点和已分配编号；
- `reconstructed_markers_3d.json`：逐帧三维Marker；
- `estimated_object_pose.json`：DLT+Kabsch初始位姿；
- `metrics.json`：检测、三维和初始位姿指标；
- `overlays/`：检测点与GT叠加图。

联合优化主要输出：

- `refined_object_pose.json`：初始和优化后位姿及诊断；
- `pose_errors.csv`：逐帧误差；
- `metrics.json`：汇总指标；
- `RESULTS.md`：自动生成的简要结果。

## 21. 总结

这套算法的核心约束是：所有Marker固定在同一个刚体上，同一帧中的全部相机观测必须由同一个6DoF位姿解释。

相机数量、相机位置、Marker数量、Marker尺寸和工作距离可以变化，但必须同步更新相机标定、刚体模板和二维检测参数。当前离线实现已经支持动态相机列表、多视图DLT、Kabsch初值和多相机6DoF联合优化；要形成完整真实系统，还需要完成无GT Marker编号、可靠同步、坏视角管理和时序跟踪。
